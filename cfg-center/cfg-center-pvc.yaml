---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "cfg-center-data-pvc"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: rook-cephfs

